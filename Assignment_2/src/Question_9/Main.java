package Question_9;

public class Main {

    /*
    Q: If a method in a subclass has the same signature as a method in its superclass with a different return type,
    will this be a problem?

    A: yes because this way you are overriding but you are changing the return type which is invalid (compile time error)
     */
}
