package Question_8;

public class Main {
    /* Q: If a method in a subclass has the same signature as a method in its superclass with the same return type,
    is the method overridden or overloaded?

    A: overridden --> because it is between super and subclass, and return type and signature are the same
     */

}
